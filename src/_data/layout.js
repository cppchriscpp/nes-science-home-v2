// Use the "layout" layout by default. eleventy is a bit rough around the edges, so this is the only way that really works.
// See: https://github.com/11ty/eleventy/issues/380
module.exports = "layout"